---
paths:
  - "**/*.php"
---

# Value Objects

Beat primitive obsession with self-validating types.

- Use VOs for easy-to-forget constraints.
- Self-validating types (`Email`, `Money`, `UserId`).
- If it exists, it is valid.
- `Money` as int cents, not float.
